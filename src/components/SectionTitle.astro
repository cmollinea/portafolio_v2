---
type Props = {
  id?: string;
};

const { id } = Astro.props;
---

<h1
  id={id}
  class='font-bold text-4xl bg-gradient-to-tr from-amber-500 via-amber-500 to-red-400 bg-clip-text text-transparent w-full text-center py-2 max-w-sm md:max-w-lg relative'
>
  <slot />
</h1>
